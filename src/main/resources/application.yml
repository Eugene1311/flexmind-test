spring:
  application:
    name: flexmind-test
  threads:
    virtual:
      enabled: true
  elasticsearch:
    username: elastic
    password: password

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG

app:
  db: elastic
